import { PlayerContext } from './playerSlice';
import { UIContext } from './uiSlice';

// PUBLIC_INTERFACE
export function usePlayer() {
  /** Hook placeholder for potential future add-ons */
  return PlayerContext;
}

// PUBLIC_INTERFACE
export function useUI() {
  /** Hook placeholder for potential future add-ons */
  return UIContext;
}
